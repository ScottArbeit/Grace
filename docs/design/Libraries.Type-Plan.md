# Library type plan

Status: accepted 2026-09-06 and implemented by the Issue #1042 replacement candidate. The inventory applies to the source reviewed at PR #1043 head `6399d4db015085a1185eb7e367a8d24ece6ddbaa`, including the inherited Library work from Issue #1038. Exact prior identifiers remain in the table for lookup.

Use [Libraries design](../Libraries.Design.md) for behavior and storage rules. This inventory contains 58 Remove, 56 Change, 55 Keep and 4 Stay removed entries. Shared types predating Libraries require stronger caller and benefit evidence before changing them. Each new declaration beyond this plan must explain the behavior and ownership it supports.

| Existing declaration | Decision | Accepted change and reason |
| --- | --- | --- |
| `LibraryControlDocument` | Change | **Change.** Keep one repository control state in RepositoryLibraryActor. Replace NextCursor, AppliedThrough and four watermarks with CommittedCursor, HistoryThrough and NotifyThrough; derive the next cursor. Keep catalog, epoch, replay floor and one Pending decision. Add exact item/slot admission counts. Remove the baseline pointer: the baseline key is deterministic from epoch, committed cursor and catalog version. |
| `LibraryPendingCommandDocument` | Change | **Change.** Rename to LibraryPendingDecision: ItemChange carries the complete accepted-change record; CatalogChange carries a deterministic add/remove delta, operation/hash, expected/chosen version and accepted time/principal against the retained catalog. Avoid two full catalogs in bounded control. Remove nested full receipt and repeated decision metadata; retain one complete recoverable choice. |
| `LibraryProjectionWatermarks` | Remove | **Remove.** Put only independently advancing positions directly on control. Current and receipt completion are the same CommittedCursor; baseline position belongs to its immutable manifest. HistoryThrough and NotifyThrough have explicitly different jobs. |
| `LibraryCanonicalChangeDocument` | Change | **Change.** Rename to `LibraryAcceptedChangeRecord`. Keep the permanent journal. Store Cursor, RequestHash, CorrelationId, one LibraryChangeDto containing the resulting item, prior namespace/content facts, consumed preconditions and the two admission-count deltas. Remove the duplicate public cursor, outer operation identity and routing fields supplied by the provider envelope. The full chosen decision exists here once after Pending clears. |
| `LibraryCurrentItemDocument` | Change | **Change.** Keep the current-item projection for direct lookup and SQL enumeration. Keep one numeric LastCursor and the item; remove AppliedThrough and inner provider routing copies. Add only the history-tail pointer needed for exact-key history traversal. Namespace is parent identity plus name, with no persisted absolute descendant path. |
| `LibraryCurrentSlotDocument` | Change | **Change.** Keep remembered occupied/vacant slots, keyed by parent identity plus normalized sibling name instead of the full path. Store Slot, LastCursor and its history-tail pointer. Remove AppliedThrough and duplicate routing fields. Vacancy versions still prevent stale creates after path reuse. |
| `LibraryReceiptDocument` | Change | **Change.** Use one immutable operation-result document for both item and catalog operations: OperationId, RequestHash and LibraryOperationOutcome. Accepted item results contain a accepted-change cursor reference; rejected item and catalog results retain their exact response. Resolve the accepted response from the immutable journal. Readers repair/check matching Pending before treating a prewritten result as terminal. |
| `LibraryCatalogOperationDocument` | Remove | **Remove.** Fold catalog outcomes into LibraryReceiptDocument and its CatalogResult outcome. The single Pending catalog case handles crash repair. Delete LatestCatalogOperation, provisional result upgrades and ControlCommitVersion rather than keeping a second catalog-specific commit protocol. |
| `LibraryHistoryEntry` | Remove | **Remove.** History segments retain accepted-change cursor references instead of copying item, path, content, tombstone, conflict and acceptance facts into a second event shape. Read those facts from the permanent accepted-change record. |
| `LibraryHistorySegmentDocument` | Change | **Change.** Keep compact exact-key history segments: PreviousSegment plus a strictly ordered array of accepted-change cursors. A segment contains at most 512 cursor positions from a fixed repository cursor window. Item/slot projections retain the latest nonempty segment pointer; links skip empty windows. Measure the real provider JSON envelope against 900 KiB. Temporal path history follows parent/slot edges. |
| `LibraryBaselineShardDocument` | Change | **Change.** Keep immutable baseline shards. Remove duplicated ItemCount and SerializedBytes fields; compute counts and serialized size when building/verifying. Keep ordered items, boundary identity and schema version where needed; provider envelope owns document routing. |
| `LibraryBaselineManifestDocument` | Change | **Change.** Keep manifest-last publication. Replace the three parallel ShardIds/ShardHashes/ShardItemCounts arrays with LibraryBaselineShardReference[]. Keep epoch, boundary, complete catalog and creation time; derive total item count from shard references and catalog version from the catalog. Baseline ID is deterministic from repository, epoch, cursor and catalog version. |
| `LibraryContentLocationDocument` | Change | **Change.** Keep one mapping from content ID to the stored descriptor, complete manifest and retained scope. It must survive UploadSession cleanup. The repository actor validates the completed upload before creating the mapping; repeated bytes reuse the existing stored value. Remove duplicate routing fields while keeping the manifest and placement checks. |
| `LibraryCurrentIndexBucketDocument` | Remove | **Remove.** Delete the current index bucket/directory records. SQL enumerates each exact current partition; two bounded control counters enforce admission without duplicating every record identity across an index tree. |
| `LibraryCurrentIndexDirectoryDocument` | Remove | **Remove.** Delete the current index bucket/directory records. SQL enumerates each exact current partition; two bounded control counters enforce admission without duplicating every record identity across an index tree. |
| `FailedGraceEventEnvelope` | Change | **Change.** Keep the exact failed transport envelope, but store at most one per repository at the fixed failed-event key. NotifyThrough identifies the oldest unsent journal change; stop notification draining at the first terminal failure. Persist only after send failure, advance NotifyThrough after successful send, then clear obsolete fallback. Namespace acceptance and history need not wait for transport recovery. |
| `GraceEventEnvelopeProperty` | Remove | **Remove.** Use the existing serializer-supported Dictionary for the envelope's currently string-valued application properties. Preserve exact values; do not replace them with object or invent conversions for future property kinds. |
| `LibraryControlRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryControlRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryCatalogOperationRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryCatalogOperationRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryCanonicalChangeRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryCanonicalChangeRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryCurrentItemRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryCurrentItemRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryCurrentSlotRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryCurrentSlotRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryCurrentIndexBucketActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryCurrentIndexBucketActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryCurrentIndexDirectoryActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryCurrentIndexDirectoryActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryReceiptRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryReceiptRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryHistorySegmentRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryHistorySegmentRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryBaselineShardRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryBaselineShardRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryBaselineManifestRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryBaselineManifestRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryContentLocationRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryContentLocationRecordActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `LibraryGraceEventFallbackActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryGraceEventFallbackActor` | Remove | **Remove.** RepositoryLibraryActor will write this record through the registered Orleans provider. Keep the record itself only where the replacement model needs its data. Test provider calls, failures and restarts before deleting the actor. Replacement code and ownership. |
| `ILibraryStore` | Remove | **Remove.** Remove this application service interface and its DI registration. Move acceptance and typed persistence orchestration into the existing repository actor and F# modules. External handlers call domain grain methods; SQL read functions remain explicit. The interface is not required by Orleans. |
| `ILibraryTransferStore` | Remove | **Remove.** Remove this application service interface and its DI registration. Move acceptance and typed persistence orchestration into the existing repository actor and F# modules. External handlers call domain grain methods; SQL read functions remain explicit. The interface is not required by Orleans. |
| `ILibraryCoordinator` | Remove | **Remove.** Remove this application service interface and its DI registration. Move acceptance and typed persistence orchestration into the existing repository actor and F# modules. External handlers call domain grain methods; SQL read functions remain explicit. The interface is not required by Orleans. |
| `ILibraryCursorCodec` | Remove | **Remove.** Replace the encode/decode interface with functions in one private LibraryTokens module. Capture the configured key once; use different authenticated token purposes. No polymorphic cursor service is needed. |
| `ILibraryWriteAuthorizer` | Remove | **Remove.** Pass a narrow authorization function to the actor composition boundary, implemented using the existing IGracePermissionEvaluator. Preserve the recheck immediately before reservation. Do not duplicate or redesign the shared permission evaluator. |
| `LibraryStoreRead` | Remove | **Remove.** Use Orleans GrainState, which already carries State, ETag and RecordExists, inside private persistence functions. A tuple may return a read-only snapshot when necessary; do not expose provider state to HTTP handlers. |
| `LibraryControlWriteResult` | Remove | **Remove.** Handle the provider's condition failure at the private write boundary and reload durable state. Use Result only if the call site needs a value. Remove the Library-specific CAS outcome union once the broad store interface is gone. |
| `IGraceEventSender` | Remove | **Remove.** Keep one sendEnvelope function over the existing Azure Service Bus SDK sender. Tests pass a function that fails or records a send. The Service Bus SDK interface/classes remain framework adapters; a parallel Grace interface and implementation add no transport capability. |
| `ServiceBusGraceEventSender` | Remove | **Remove.** Keep one sendEnvelope function over the existing Azure Service Bus SDK sender. Tests pass a function that fails or records a send. The Service Bus SDK interface/classes remain framework adapters; a parallel Grace interface and implementation add no transport capability. |
| `ILibraryManifestContributionActivator` | Remove | **Remove.** Replace the activator class/interface and production no-op with two explicit functions: retainAcceptedContent and acknowledgeRetention. They call the existing counter and manifest workflow owners. Keep the tracked-add protocol and fail when required wiring is absent; deleting this seam must not delete the durable handoff. |
| `ManifestContributionActivator` | Remove | **Remove.** Replace the activator class/interface and production no-op with two explicit functions: retainAcceptedContent and acknowledgeRetention. They call the existing counter and manifest workflow owners. Keep the tracked-add protocol and fail when required wiring is absent; deleting this seam must not delete the durable handoff. |
| `NoopLibraryManifestContributionActivator` | Remove | **Remove.** Replace the activator class/interface and production no-op with two explicit functions: retainAcceptedContent and acknowledgeRetention. They call the existing counter and manifest workflow owners. Keep the tracked-add protocol and fail when required wiring is absent; deleting this seam must not delete the durable handoff. |
| `Coordinator` | Remove | **Remove.** Move this class's behavior into a LibraryDecision module and RepositoryLibraryActor's serialized methods. Separate pure validation/decision functions from ordered effects. Do not recreate Coordinator as a record containing every old store method. |
| `LibraryCursorCodec` | Remove | **Remove.** Consolidate cursor and page/read token implementations into the LibraryTokens module; keep purpose and repository binding, HMAC verification, fixed expiry and tamper rejection. Remove the two application classes. |
| `LibraryTokenPayload` | Keep | **Keep.** the private authenticated token payload. Kind, repository, value, offset and expiry describe one real technical protocol; it is transient signed data, not an actor or persisted grant. |
| `LibraryOpaqueTokenCodec` | Remove | **Remove.** Consolidate cursor and page/read token implementations into the LibraryTokens module; keep purpose and repository binding, HMAC verification, fixed expiry and tamper rejection. Remove the two application classes. |
| `OrleansLibraryStore` | Remove | **Remove.** Replace the broad actor-forwarding store class with private typed Orleans record functions and separate explicit Cosmos SQL functions. Remove the forwarding methods along with the record actors. |
| `GraceDocumentIdProvider` | Change | **Change.** Keep one parameterized IDocumentIdProvider implementation, constructed with the validated key depth for each named provider. This is a framework-required adapter. Validate key components and keep exact provider registration/serialization tests. |
| `LibraryControlDocumentIdProvider` | Remove | **Remove.** Register GraceDocumentIdProvider(options, depth) through a DI factory. A named storage purpose does not need a subclass whose only difference is 1, 2 or 3. |
| `LibraryTwoLevelDocumentIdProvider` | Remove | **Remove.** Register GraceDocumentIdProvider(options, depth) through a DI factory. A named storage purpose does not need a subclass whose only difference is 1, 2 or 3. |
| `LibraryThreeLevelDocumentIdProvider` | Remove | **Remove.** Register GraceDocumentIdProvider(options, depth) through a DI factory. A named storage purpose does not need a subclass whose only difference is 1, 2 or 3. |
| `GracePartitionKeyProvider` | Keep | **Keep.** the established GracePartitionKeyProvider for existing non-Library actors. The approved HPK adapter owns Library keys. Removing or changing this shared provider would affect unrelated persisted actors without a Library benefit. |
| `GraceEventDelivery.Result` | Keep | **Keep.** the two-case Delivered/Deferred result for the send-first helper. It names two different effects clearly and remains a tiny transient union. Do not collapse useful domain/effect distinctions just to reduce the count. |
| `LibraryUploadPreparation` | Change | **Change.** Keep the optional Library binding inside UploadSession, but store only OperationId, PrincipalId, expected SHA-256 and the fixed preparation expiry. Reuse the session's ID, BLAKE3 hash, size, scope and pool; derive the response instead of persisting an entire DTO plus JSON upload instructions. Add explicit Orleans field IDs. |
| `StartUploadSession` | Change | **Change.** Keep the existing upload lifecycle and the optional Library binding. Add/verify serialization IDs on every field crossing Orleans, including nested values. Derive the Library preparation response from persisted session facts; remove the empty-codec fallback path. Preserve generic upload callers and event replay. |
| `UploadSessionEventType` | Change | **Change.** Keep the existing upload lifecycle and the optional Library binding. Add/verify serialization IDs on every field crossing Orleans, including nested values. Derive the Library preparation response from persisted session facts; remove the empty-codec fallback path. Preserve generic upload callers and event replay. |
| `UploadSessionDto` | Change | **Change.** Keep the existing upload lifecycle and the optional Library binding. Add/verify serialization IDs on every field crossing Orleans, including nested values. Derive the Library preparation response from persisted session facts; remove the empty-codec fallback path. Preserve generic upload callers and event replay. |
| `PhysicalDeletionReminderState` | Keep | **Keep.** the existing upload owner and the small cleanup decision shape. Exact generation and StartedAt + LogicalDeleteDays guard physical session-state cleanup. This removes temporary metadata, never accepted historical bytes. No new Library upload actor or lifecycle is justified. |
| `CleanupReminderDisposition` | Keep | **Keep.** the existing upload owner and the small cleanup decision shape. Exact generation and StartedAt + LogicalDeleteDays guard physical session-state cleanup. This removes temporary metadata, never accepted historical bytes. No new Library upload actor or lifecycle is justified. |
| `UploadSessionActor` | Keep | **Keep.** the existing upload owner and the small cleanup decision shape. Exact generation and StartedAt + LogicalDeleteDays guard physical session-state cleanup. This removes temporary metadata, never accepted historical bytes. No new Library upload actor or lifecycle is justified. |
| `IRepositoryLibraryActor` | Change | **Change.** Keep the single repository-scoped Library grain interface and actor. Move catalog admission, submit/retry, bounded record writes, current-read publication checks, baseline construction and bounded recovery here. Replace the record-actor forest with typed provider calls. Preserve IsInLibrary as the narrow read used by existing version-control domains. |
| `RepositoryLibraryActor` | Change | **Change.** Keep the single repository-scoped Library grain interface and actor. Move catalog admission, submit/retry, bounded record writes, current-read publication checks, baseline construction and bounded recovery here. Replace the record-actor forest with typed provider calls. Preserve IsInLibrary as the narrow read used by existing version-control domains. |
| `IRepositoryContentCounterActor` | Keep | **Keep.** the existing repository content-counter contract, snapshot and completed-change type, including PendingTrackedAdd. Counter callers include ordinary content references as well as Library acceptance; the retained exact tracked transition survives intervening references and cache loss. Library historical references remain permanent, one contribution per accepted content-bearing operation. |
| `RepositoryContentCounterActor` | Keep | **Keep.** the existing repository content-counter contract, snapshot and completed-change type, including PendingTrackedAdd. Counter callers include ordinary content references as well as Library acceptance; the retained exact tracked transition survives intervening references and cache loss. Library historical references remain permanent, one contribution per accepted content-bearing operation. |
| `RepositoryContentCounterDto` | Keep | **Keep.** the existing repository content-counter contract, snapshot and completed-change type, including PendingTrackedAdd. Counter callers include ordinary content references as well as Library acceptance; the retained exact tracked transition survives intervening references and cache loss. Library historical references remain permanent, one contribution per accepted content-bearing operation. |
| `RepositoryContentCounterCompletedChange` | Keep | **Keep.** the existing repository content-counter contract, snapshot and completed-change type, including PendingTrackedAdd. Counter callers include ordinary content references as well as Library acceptance; the retained exact tracked transition survives intervening references and cache loss. Library historical references remain permanent, one contribution per accepted content-bearing operation. |
| `ContentBlock` | Keep | **Keep.** the shared ContentBlock/FileManifest shapes and the added Orleans IDs. Generic upload, manifest completion, contribution workflows and materialization need their complete identity and placement facts. Library-local cleanup does not justify redesigning these established contracts. |
| `FileManifest` | Keep | **Keep.** the shared ContentBlock/FileManifest shapes and the added Orleans IDs. Generic upload, manifest completion, contribution workflows and materialization need their complete identity and placement facts. Library-local cleanup does not justify redesigning these established contracts. |
| `GraceEvent` | Keep | **Keep.** GraceEvent and its LibraryContentAvailableEvent case. It is the established cross-domain event envelope; removal would fragment the existing event pipeline. Generate the Library payload from accepted journal facts. |
| `Startup` | Change | **Change.** Keep server entry/composition types. Replace Library service/store/record-grain wiring with the repository actor, functional authorization/send boundaries, six named providers and a shared CosmosClient. Update indexes for the final State JSON paths. Preserve unrelated server and provider registrations. |
| `Program` | Change | **Change.** Keep server entry/composition types. Replace Library service/store/record-grain wiring with the repository actor, functional authorization/send boundaries, six named providers and a shared CosmosClient. Update indexes for the final State JSON paths. Preserve unrelated server and provider registrations. |
| `LibraryParentDto` | Keep | **Keep.** the small public parent shape, with one validated root-path or parent-item identity. Validate its Kind/options exactly once at admission. A separate LibraryId or parent actor is unnecessary; typed internal matching can reuse existing options/results. |
| `LibraryNamespaceDto` | Change | **Change.** Keep only Parent, Name and NamespaceVersion. Remove persisted NormalizedPath and the duplicate SlotVersion. Compute full paths from the parent graph at the relevant cursor; slot versions belong to slot reads and slot preconditions. |
| `LibraryContentVersionDto` | Keep | **Keep.** immutable byte identity, BLAKE3, SHA-256, size and the first stored CreatedAt. Equal bytes reuse the same descriptor. This identifies content, not necessarily the causal edit revision; the separate concurrency choice is explicit in the redesign. |
| `LibraryTombstoneDto` | Change | **Change.** Keep deletion facts but remove repeated ItemId/ItemKind. Retain LastNamespace (parent/name/version) and LastContentVersionId with DeletedAt, DeletedBy and DeleteCursor, so tombstoned current items still carry their last edge without restoring live content. |
| `LibraryConflictProvenanceDto` | Change | **Change.** Keep only the causal facts not already in the accepted change: OriginalItemId and the submitted base content identity/revision. Derive the new conflict item, its name/content, source operation and acceptance time from Change.Item and the outer change. No separate conflict record or actor. |
| `LibraryItemDto` | Change | **Change.** Keep ItemId, ItemKind, LastChangeCursor, Namespace, Content and Tombstone; derive live/tombstoned state and remove per-item catalog-version duplication. Enforce the valid file/directory/tombstone combinations at construction. Add the owner-approved causal ContentRevision field using LibraryCursor; it is independent of content-addressed storage identity. |
| `LibraryNamespaceSlotDto` | Change | **Change.** Keep Parent, Name, SlotVersion and OccupantItemId. Derive occupied/vacant state; remove NormalizedPath. Key and equality are normalized parent identity plus NFC OrdinalIgnoreCase name, so ancestor moves do not rewrite descendant slots. |
| `LibraryCatalogDto` | Keep | **Keep.** the versioned complete catalog and its provenance. One normalized configured path is one Library; no per-Library identity object, actor or catalog-entry class is added. 128-root and non-overlap limits remain. |
| `LibraryNamespacePreconditionDto` | Keep | **Keep.** the named precondition record because it groups one meaningful independent comparison. Namespace revision and destination vacancy are different concurrency facts; flattening these fields would not remove that complexity or improve safety. |
| `LibraryContentPreconditionDto` | Change | **Change.** Keep one content precondition. Owner accepted ExpectedContentRevision to detect intervening edits, with ExpectedContentVersionId identifying the submitted base bytes. Compare the revision for update/delete concurrency; identical X bytes may share ContentVersionId while r1/X and r3/X remain different editing bases. |
| `LibraryCreationSlotExpectationDto` | Keep | **Keep.** the named precondition record because it groups one meaningful independent comparison. Namespace revision and destination vacancy are different concurrency facts; flattening these fields would not remove that complexity or improve safety. |
| `LibraryChangeDto` | Change | **Change.** Keep one accepted-change envelope with operation, kind, actor/time, catalog version, resulting Item and optional compact Conflict. Remove parallel ItemId/ItemKind/Namespace/Content/Tombstone fields that repeat Item. Derive the public cursor from Item.LastChangeCursor. Accepted-change storage adds only private replay/admission facts. |
| `LibraryRebaselineDto` | Keep | **Keep.** explicit rebaseline reason, current epoch and service floor. It is an actionable client outcome with a distinct recovery behavior, not another persistent state machine. |
| `LibraryOperationReceiptDto` | Change | **Change.** Keep operation/hash/outcome and one accepted Change or one rejection explanation/catalog/rebaseline result. Remove the duplicate Item, Cursor, Conflict and accepted metadata fields already in Change. Persist accepted receipts as journal references and reconstruct the exact response; persist rejection responses once. |
| `LibraryCatalogChangeResultDto` | Keep | **Keep.** the catalog operation's public result, including the complete catalog and recorded outcome. Its shape differs usefully from an item change. Persist it inside the unified operation result instead of a separate catalog-operation document family. |
| `LibraryWriteAuthorization` | Keep | **Keep.** the authenticated authorization input record; it carries verified principals/claims across the existing actor call. It is transient input, not a permission decision. Recheck against the shared evaluator at reservation. |
| `LibrarySubmitResult` | Remove | **Remove.** Use the existing F# Result (or the existing denied result) at the actor boundary. Two independent options currently allow both/neither receipt and forbidden reason; a named wrapper buys no additional meaning. |
| `LibraryBootstrapPageDto` | Keep | **Keep.** the public bootstrap page. It is one bounded transport response with catalog, boundary and item page. Build a baseline under the owner-approved write pause, then serve pages from immutable shards without holding the actor turn. |
| `LibraryChangePageDto` | Keep | **Keep.** the change page and explicit rebaseline outcome. Query only accepted positions through the captured committed boundary. Paging must not expose a accepted-change record whose synchronous publication is still pending. |
| `LibraryPreparedContentDto` | Change | **Change.** Rename to LibraryContentPreparationDto. Return the existing UploadSessionId, hashes, size, fixed expiry, AuthorizedScope and StoragePoolId as typed fields. Remove the second PreparedContentId identity, always-true UploadRequired and JSON-encoded UploadInstructions. Route paths belong in SDK methods; reuse the existing upload protocol. |
| `LibraryContentReadGrantDto` | Change | **Change.** Rename to LibraryContentReadDto; keep DownloadPath, the stored Content descriptor and ExpiresAt. Remove the duplicate GrantId token and one-use vocabulary. The signed URL binds repository plus immutable content identity and remains valid for that version until expiry, with existing authorization/no-oracle checks. |
| `LibraryRepositoryStatusDto` | Change | **Change.** Keep server status based on completed work. Derive pending count (0/1), oldest pending time, committed position and history/notification lag from control and Pending rather than storing a parallel status model. Do not label the repository caught up while a required synchronous decision is unfinished. |
| `LibraryContentAvailable` | Keep | **Keep.** the small coarse wake event. It exposes repository/cursor/catalog progress without content, and existing notification subscribers already understand its role. It is a hint; the journal and client cursor remain the sources used for recovery. |
| `LibraryChangeCommand` | Change | **Change.** Replace the option-heavy resolved command with a validated LibraryChangeCommand union for create-file, create-directory, content-update, rename, move and delete. Carry only request facts and UploadSessionId; resolve the temporary upload and retained content inside the actor after receipt lookup. Remove pre-resolved content/expiry and duplicate repository identity from trusted internal calls. |
| `LibraryItemId` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryOperationId` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryCatalogVersion` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryNamespaceVersion` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryNamespaceSlotVersion` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryContentVersionId` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryBootstrapId` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryPreparedContentId` | Remove | **Remove.** Reuse the existing UploadSessionId type. The prepared-content ID already identifies that same session, so a Library-specific alias and duplicate wire field imply a second identity that does not exist. |
| `LibraryCursor` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryCursorEpoch` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryPageToken` | Keep | **Keep.** this descriptive F# abbreviation. It adds no runtime owner, allocation, persistence record or workflow. Use it where it makes a contract readable; do not replace cheap meaningful names with interchangeable raw primitives merely to lower the declaration count. |
| `LibraryControlState` | Remove | **Remove.** Replace this interface-specific fake with test functions and the single test-only IGrainStorage fixture. Keep stored state separate from each attempt’s working state. Exercise the real counter’s retry behavior; a fake that remembers every operation forever cannot check it. Replacement tests. |
| `LibraryContentLocationState` | Remove | **Remove.** Replace this interface-specific fake with test functions and the single test-only IGrainStorage fixture. Keep stored state separate from each attempt’s working state. Exercise the real counter’s retry behavior; a fake that remembers every operation forever cannot check it. Replacement tests. |
| `RecordingLibraryManifestContributionActivator` | Remove | **Remove.** Replace this interface-specific fake with test functions and the single test-only IGrainStorage fixture. Keep stored state separate from each attempt’s working state. Exercise the real counter’s retry behavior; a fake that remembers every operation forever cannot check it. Replacement tests. |
| `NoopLibraryManifestContributionActivator` | Remove | **Remove.** Replace this interface-specific fake with test functions and the single test-only IGrainStorage fixture. Keep stored state separate from each attempt’s working state. Exercise the real counter’s retry behavior; a fake that remembers every operation forever cannot check it. Replacement tests. |
| `LibraryGraceEventDeliveryTests` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `FailingLibraryStore` | Remove | **Remove.** Replace this interface-specific fake with test functions and the single test-only IGrainStorage fixture. Keep stored state separate from each attempt’s working state. Exercise the real counter’s retry behavior; a fake that remembers every operation forever cannot check it. Replacement tests. |
| `LibraryCoordinatorTests` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `OrleansPartitionKeyProviderTests` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `RepositoryContentCounterActorTests` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `UploadSessionActorTests` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `StorageContentBlockSdkContract` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `StorageManifestUploadSessionRoutes` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `OperationalFactsPublisherAppHostTests` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `LibraryTypesTests` | Change | **Change.** Keep the useful behavior tests and update them for the new records and functions. Test storage failures, restarts, serialization and callers; remove tests of deleted forwarding wrappers. Required test cases. |
| `RecordingTransferStore` | Stay removed | **Stay removed.** Keep this removal. Do not restore a direct Cosmos mutation store, a Library-only prepared-content record or a consumed one-use read-grant record. Reuse Orleans persistence, UploadSession and stateless signed reads. |
| `CosmosLibraryStore` | Stay removed | **Stay removed.** Keep this removal. Do not restore a direct Cosmos mutation store, a Library-only prepared-content record or a consumed one-use read-grant record. Reuse Orleans persistence, UploadSession and stateless signed reads. |
| `LibraryPreparedContentDocument` | Stay removed | **Stay removed.** Keep this removal. Do not restore a direct Cosmos mutation store, a Library-only prepared-content record or a consumed one-use read-grant record. Reuse Orleans persistence, UploadSession and stateless signed reads. |
| `LibraryContentReadGrantDocument` | Stay removed | **Stay removed.** Keep this removal. Do not restore a direct Cosmos mutation store, a Library-only prepared-content record or a consumed one-use read-grant record. Reuse Orleans persistence, UploadSession and stateless signed reads. |
| `LibraryParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `GetLibraryCatalogParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `ListLibrariesParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `AddLibraryParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `RemoveLibraryParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `StartLibraryBootstrapParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `ContinueLibraryBootstrapParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `GetLibraryChangesParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `SubmitLibraryChangeParameters` | Change | **Change.** Keep the HTTP parameter adapter and validate it into LibraryChangeCommand once. Replace PreparedContentId with UploadSessionId, use parent/name slot identity, and apply the chosen content revision precondition. Generate clients and examples from this same final contract. |
| `GetLibraryOperationParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `PrepareLibraryContentParameters` | Change | **Change.** Keep the prepare endpoint parameters (operation, hashes, size). Its response uses existing upload-session identity and typed upload facts. Retry binds repository, operation and principal and never creates a second preparation lifecycle. |
| `PrepareLibraryContentReadParameters` | Change | **Change.** Keep the read request and add ContentRevision. Point-read that accepted-change record and verify repository, item and content identity, independently of the current head. An unaccepted retained-location residue is not sufficient. Mint one short-lived immutable-version URL; later head changes do not revoke that capability. |
| `GetLibraryItemParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `GetLibraryNamespaceSlotParameters` | Change | **Change.** Keep the slot lookup route but address the slot by parent identity and normalized name. If the caller starts with a full path, resolve its segments against the parent graph first; never use a stale descendant path as the slot identity. |
| `GetLibraryStatusParameters` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `GetLibrary` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `ListLibraries` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `AddLibrary` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `RemoveLibrary` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `Libraries` | Keep | **Keep.** Retain the route/CLI/SDK adapter required by Grace's established public surface. Shared base parameters and small command classes match surrounding conventions. Adjust member signatures and generated outputs where the accepted DTOs change; do not collapse distinct public operations into an untyped request. |
| `LibraryItemKind` | Keep | **Keep.** the generated enum/schema contract where it defines meaningful allowed public values. Update values only for an actual contract change. These generated shapes are not independently authored domain entities. |
| `LibraryChangeKind` | Keep | **Keep.** the generated enum/schema contract where it defines meaningful allowed public values. Update values only for an actual contract change. These generated shapes are not independently authored domain entities. |
| `LibraryOutcomeKind` | Keep | **Keep.** the generated enum/schema contract where it defines meaningful allowed public values. Update values only for an actual contract change. These generated shapes are not independently authored domain entities. |
| `LibraryRejectionReason` | Keep | **Keep.** the generated enum/schema contract where it defines meaningful allowed public values. Update values only for an actual contract change. These generated shapes are not independently authored domain entities. |
| `LibraryCatalogRejectionReason` | Keep | **Keep.** the generated enum/schema contract where it defines meaningful allowed public values. Update values only for an actual contract change. These generated shapes are not independently authored domain entities. |
| `LibraryCatalogReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryCatalogChangeReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryBootstrapPageReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryChangePageReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryOperationReceiptReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryPreparedContentReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryContentReadGrantReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryItemReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryNamespaceSlotReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryStatusReturnValue` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |
| `LibraryReturnValueBase` | Change | **Change.** Regenerate this schema/return wrapper from the redesigned source DTO and the existing Grace response convention. Remove fields/types whose source contracts disappear, rename preparation/read wrappers consistently, and retain required language-specific response adapters. Do not hand-edit generated types or count regenerated helpers as new domain decisions. |

## Additional shared and serializer-facing declarations

These entries supplement the 173-entry inventory. Existing shared owners are retained unless this table identifies an accepted change.

| Type / shape | Decision | Source | Responsibility, ownership, identity, lifecycle and reason |
| --- | --- | --- | --- |
| LibraryWriteAuthorizer | Remove | [Issue #1038 adapter](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Server/Security/PermissionEvaluator.Server.fs#L73) | Server composition creates it; the repository actor invokes it; DI owns its process lifetime. No key, partition or durable state. It only forwards to IGracePermissionEvaluator. Replace it and ILibraryWriteAuthorizer with a narrow function. Issue #1038 required the authorization recheck, not this class; Issue #1042 does not require it. |
| RepositoryActor | Keep | [existing owner, changed by Issue #1038](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Actors/Repository.Actor.fs#L89) | One actor per repository, existing repository event storage/partition and repository lifetime. Its create workflow initializes Library catalog from immutable creation metadata; Library cannot call it back during that turn. Existing repository commands/readers retain their owner. Keep this small caller change; merging Library state into the repository event list would couple high-cardinality history to an established domain. |
| Authorization.Operation | Keep | [shared union](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Types/Authorization.Types.fs#L62) | Keep LibraryRead/LibraryWrite cases added by Issue #1038. Route authorization, shared role bundles and repository permission evaluation consume them. This is a serialized permission vocabulary, not a new Library owner; access-control records retain their existing key/storage/lifetime. Removal would collapse an explicit product authorization distinction. Issue #1042 preserves it. |
| RepositoryPathClassification | Keep | [shared classifier union](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.CLI/Command/Services.CLI.fs#L1041) | Keep the Library case. Scans and Watch create/read this transient classification; no independent CRUD, partition or persistence. It excludes Library-owned paths from version-controlled content. Issue #1038 requires this boundary; combining Library with Ignored loses the reason and ownership distinction. |
| RepositoryPathClassifierInput | Keep | [shared classifier input](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.CLI/Command/Services.CLI.fs#L1054) | Keep the copied Libraries array for one operation. Commands build the input; scans and Watch read it, then discard it. It describes the catalog snapshot used by that operation and does not decide catalog changes. It has no key, partition or stored record. |
| WorkingTreeScanInput | Keep | [shared scan input](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.CLI/Command/Services.CLI.fs#L1254) | Keep the Libraries snapshot passed into one bounded scan. Existing CLI capture and scan callers own it, with operation lifetime and no persistence. It prevents a scan silently reading a different ambient policy midway. Do not redesign established scanning types to avoid this one useful field. |
| AuthorizationSemanticsTests | Change | [existing fixture](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Authorization.Tests/AuthorizationSemantics.Tests.fs#L15) | Keep the test class and Library permission checks. Update fixtures for the revised routes and DTOs. NUnit creates and disposes it; it has no domain identity or stored state. The required behavior is worth testing without adding a production interface just for the tests. |
| EndpointAuthorizationManifestTests | Change | [existing fixture](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Authorization.Tests/EndpointAuthorizationManifest.Tests.fs#L12) | Keep Library route authorization coverage and update renamed read/preparation contracts. NUnit lifetime; no durable key. It protects no-oracle/read/write boundaries required by Issue #1038 and preserved by Issue #1042. |
| OpenApiRouteCoverageTests | Change | [existing fixture](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Authorization.Tests/OpenApiRouteCoverage.Tests.fs#L20) | Keep schema/route propagation checks against the regenerated contract. Existing Route and ClassificationGroup test records stay: they describe test input, have fixture lifetime and no domain owner/partition. These shapes are test adapters, not reasons for more Library runtime types. |
| NotificationServerTests | Change | [existing fixture](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Server.Unit.Tests/Notification.Server.Tests.fs#L22) | Keep tests for permissions, wake notifications, duplicate or reordered messages, and accepted-change replay. Update them for NotifyThrough and the single failed-envelope record. Message delivery does not establish that the client has applied the files. |
| Repository (server test fixture) | Change | [existing route fixture](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Server.Tests/Repository.Server.Tests.fs#L26) | Keep repository-create/catalog-initialization coverage added by Issue #1038. Test host creates/cleans repository fixtures using existing APIs. Test data uses existing repository keys/stores; no new Library type is necessary. |
| Anonymous upload-instructions record | Remove | [serializer-facing anonymous shape](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.Server/Library.Server.fs#L592) | The prepare handler constructs UploadSessionId/scope/pool/three route strings, serializes it into a string, then stores it in a DTO nested inside UploadSession. It duplicates session facts and SDK routing. Replace with the typed preparation response fields; do not give the anonymous shape a new permanent class. |
| LibraryCliParsingTests (module) | Keep | [Issue #1038 test module](https://github.com/ScottArbeit/Grace/blob/6399d4db015085a1185eb7e367a8d24ece6ddbaa/src/Grace.CLI.Tests/Library.CLI.Parsing.Tests.fs#L12) | This is a source module compiled into a CLR holder, not a hand-authored F# type. Keep its parser and SDK checks and update renamed members. It exists only during test execution and stores no Library state. It is listed so the generated CLR holder is not mistaken for a domain type. |

| Existing dependency | Required use and lifetime | Recommendation |
| --- | --- | --- |
| UploadSessionLifecycleState; UploadSessionCommand; UploadSessionEvent; generic upload request/range records | Generic manifest upload and Library transfer both use the same session. Session identity/partition, event replay, completion, abandonment, expiry and cleanup remain with UploadSessionActor. Library adds only the small optional binding. | Keep the existing upload session. A second Library transfer object would duplicate its completed manifest and cleanup decisions. Check serialization through the actual callers without changing unrelated upload behavior. |
| RepositoryContentCounterCommand, EventType, Intent, Event, Decision; lifecycle and change-operation unions | Ordinary references and Library historical content share the repository/pool/manifest counter. Counter decision/intents drive the existing zero-transition workflow; event/snapshot durability belongs to that actor. | Keep. Retain PendingTrackedAdd and its existing completed-change record. The real cache-loss/intervening-add failure justifies the shared repair; declaration count does not justify replacing it. |
| ManifestContributionDirection, WorkflowLifecycleState, Range, StartManifestContributionWorkflow, RangeProgress, RangeFailure, Command, EventType, Intent, Event, Dto, Decision; IManifestContributionWorkflowActor / implementation | The existing repository/pool/manifest workflow owns bounded range activation progress and retries. Range/content actors own immutable byte availability. Library consumes completion through the existing API. | Keep these shared shapes. Move only the Library adapter into functions. An acceptance must not invent a “retained” Boolean that bypasses their actual completion evidence. |
| IGracePermissionEvaluator, GracePermissionEvaluator, PermissionCheckResult, Principal/Resource and existing role records | The existing evaluator checks current roles and claims. Library supplies authenticated user information and the repository resource before reservation. A denied request makes no Library change. | Keep. Remove the extra LibraryWriteAuthorizer forwarding class; do not redesign repository authorization to save one local dependency. |
| WDU CallerKind, Target, Operation, AttemptToken, PreparedManifest/Entry, PreparedContent, Request, Receipt, Progress, Failure and Outcome | Established Branch/Watch/Connect publication contracts and durable local completion. Shared mechanical filesystem/root-exclusion functions can be reused. | Keep. They do not describe Library cursor/item ancestry. Use the accepted Library local rows with the existing mechanics; do not generalize WDU or create a neutral workflow kernel. |
| `IGrainStorage`; `GrainState<T>`; `IDocumentIdProvider`; `CosmosDocumentKey`; `IGrainFactory`; `ServiceBusSender` | Existing framework contracts. Typed provider state is attempt-local; providers/client/sender have DI/runtime lifetime; GrainId selects deterministic record key. These are technical adapters with framework owners, not Library domain state. | Use these existing framework interfaces directly where needed. Test the actual provider keys and serializers, then remove application wrappers that repeat the same operations. |
