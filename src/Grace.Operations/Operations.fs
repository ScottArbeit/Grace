namespace Grace.Operations

/// Identifies the Grace operations core assembly without exposing a runtime contract before the tracer bullet defines it.
[<AbstractClass; Sealed>]
type internal OperationsAssembly =
    class
    end
