namespace Grace.CLI.Tests

/// Groups program coverage for the CLI test project.
module Program =
    /// Defines the test assembly entry point required by the F# test project used by the CLI program scenario.
    [<EntryPoint>]
    let main _ = 0
