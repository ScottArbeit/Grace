# Grace workflow

This reference routes to the current repository process instead of restating it.

Read root and nearest AGENTS.md and docs/Development process.md for tracked work. Use the installed dev-process skill and its references/QUALITY-CONTRACTS.md, references/CODE_REVIEW.md, and references/TEMPLATES.md for the portable process. Apply current owner decisions and permitted exceptions.

## Tracker and delivery

GitHub Issues and PRs are the durable implementation record. A request to plan does not publish tracker records. When tracker setup is authorized, use Grace's current task template, user benefit, owned/sensitive paths, readiness, validation, docsimpact, and stop conditions.

For epics, create the earliest tracer and only demonstrated prerequisites, with native parent relationships using the repository's addSubIssue procedure. Select delivery mode separately from issue hierarchy. An independent slice normally branches from current origin/main; an integration branch requires the documented owner decision and proof conditions.

Follow the repository's branch naming rule subject to higher-priority host and user instructions. Pin the exact base and inspect both issue and eventual delivery deltas. Use the current issue closing/link wording for the selected PR target.

## Execution and review

Freeze the required charter before semantic work. Use direct mode for bounded settled work and one issue-owner worker for Tier 2 or complicated integration. Follow the current role/model settings. Only the root spawns; fresh tracked agents use a complete packet and fork_turns = "none". Follow the documented narrow scout exception only when it applies.

Keep one continuing implementation owner. A matching startup check proceeds immediately. R1 produces one finite ledger; accepted repairs return to that owner; R2 covers closure and direct regressions. There is no automatic R3. Required Validate must pass on the final head, with owner approval of the unchanged revision and any required Shape Review or recorded waiver.

## Validation and landing

Select the repository's focused profile: docs-only, domain-contract, cli-command, server-api, actor-workflow, sdk-client, or deployment-runtime. Read [tests.md](tests.md) only when selecting or changing tests. Avoid copying a menu of broad commands into every task.

Reuse applicable evidence while its inputs remain unchanged. Establish admissibility of an unproved composition before semantic work; an identical-tree result can cover it when the relevant inputs match.

After authorized merge, verify destination content and tracker state, then remove only the completed issue's branches/worktree, fetch with prune, and update local main by fast-forward when its working state permits. Preserve unrelated work. Run the required epic checkpoint with fresh context; creation of a new visible task must be within user authorization.

For a material contract/base/process change or DISCOVERY ESCAPE, use dev-process's owner decision packet and preserve a salvage map. A phase transition or routine correction is not itself a stop condition.
